# ==========================================
# GPS Community Backend - Test Environment
# ==========================================

NODE_ENV=test

# MongoDB Test Database
MONGODB_URI_TEST=mongodb+srv://milith0dev_db_user:1997281qA@cluster0.cpt00yd.mongodb.net/gpscommunity_test?retryWrites=true&w=majority&appName=Cluster0

# JWT Configuration (test keys)
JWT_SECRET=test-secret-key-for-testing-only
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=1d

# Disable external services in tests
EMAIL_ENABLED=false
FIREBASE_ENABLED=false
REDIS_ENABLED=false
WS_ENABLED=false

# Logging
LOG_LEVEL=error
LOG_FORMAT=combined

# Security
BCRYPT_ROUNDS=4

# Rate Limiting (más permisivo en tests)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=1000
